# 집합(Jiphap) — 아키텍처

전남대학교 러닝 동아리(CNRC) GPS 출석 체크 웹앱.

이 문서는 **결정된 사항의 기록**이다. 작업 중 판단이 필요할 때 여기를 먼저 본다.
여기에 없는 것은 아직 결정되지 않은 것이고, 임의로 정하지 말고 물어본다.

---

## 1. 한 줄 정의

정해진 시각의 앞뒤 창 안에, 정해진 장소의 반경 안에 있을 때만 출석 버튼이 눌린다.
나머지는 전부 부차적이다.

---

## 2. 기술 스택과 근거

| 층 | 선택 | 근거 |
|---|---|---|
| 언어 | TypeScript | — |
| 프레임워크 | Next.js 16 (App Router) | 사용자·운영자 화면 한 코드베이스, Server Action으로 서버 검증. 16은 현재 안정 버전이고 Node 20.9+(Active LTS)를 요구한다. 주의사항은 §15 |
| 호스팅 | Vercel | HTTPS 자동(Geolocation 필수 조건), GitHub 연동 배포 |
| 백엔드 | Supabase | 카카오 로그인 턴키, Postgres, Realtime |
| 위치 | 브라우저 Geolocation API | 폰에서 네이티브와 같은 GPS 칩. 설치 불필요 |
| 지도 | 카카오맵 JS SDK | 국내 도보 경로. 구글맵은 국내 규제로 도보 안내 불가 |
| 스타일 | Tailwind CSS | — |
| 테스트 | Vitest | 순수 함수 테스트에 설정이 가장 적다. `lib/geo.ts`·`window.ts`·`gate.ts`가 전부 순수 함수라 러너 이상의 것이 필요 없다 |

### 왜 네이티브 앱이 아닌가

- 설치 마찰 제거가 가장 큰 이득. 신입이 출첵하려고 앱을 깔 이유가 없다
- 카카오맵 SDK는 JavaScript 전용. RN에서는 WebView 편법이 필요
- 백그라운드 위치가 애초에 불필요 — 앱을 열어 버튼을 누르는 것이 출첵 행위 자체
- 대가: 푸시 알림 불가. 단톡방 공지로 대체

---

## 3. 신뢰 경계 (가장 중요)

**GPS 좌표는 클라이언트가 주장하는 값이고, 서버는 이를 검증할 수 없다.**
브라우저 콘솔에서 `navigator.geolocation`을 덮어쓰면 침대에서 출첵된다.
어떤 백엔드를 쓰든, 로그인을 넣든 이것은 막히지 않는다.

이 앱의 보안 모델은 **"부정 방지"가 아니라 "정직한 사람의 실수 방지 + 낮은 수준의 억제"**다.
정교한 방어에 시간을 쓰지 말고, 아래에 투자한다.

- 참석자 명단은 운영자만 조회 → 미참석자를 차집합으로 역산할 수 없다 (§17)
- 운영자 수동 출첵 → 배터리·GPS 문제로 억울한 결석이 생기지 않는다

### 대리 출첵 억제 — 이상 탐지로 대체

**"명단 전체 공개"는 더 이상 억제 장치가 아니다.** 번개러닝을 도입하면서 참석자 명단을
운영자 전용으로 닫았다 (§17). 공개가 주던 사회적 억제가 사라지므로, 그 자리를
**운영자 전용 이상 탐지**로 대체한다. 판단 재료는 세 가지다.

- 동일 세션 내 체크인 시각이 비정상적으로 근접한 레코드
- 거리 / 정확도 분포의 이상치
- 동일 회원의 연속 세션 간 패턴 이탈

탐지 로직 구현은 별도 티켓이다 (T48). 여기에는 근거만 남긴다.

### 서버가 실제로 할 수 있는 검증

| 검증 | 방법 |
|---|---|
| 시각이 창 안인가 | 서버 시계 (`now()`). 폰 시계는 조작 가능 |
| 중복 출첵 | `unique(session_id, member_id)` |
| 회원 자격 | `profiles.status = 'active'` |
| 세션 유효성 | 세션 존재 · 미취소 |
| 거리 재계산 | 클라이언트가 보낸 좌표로 서버가 다시 계산 (좌표 자체는 못 믿음) |

### 출첵이 성립하는 조건

**승인된 회원 · 시간 창 안 · 반경 안** 세 가지다. 서버(T21)가 이 셋을 모두 확인한 뒤에만
`check_ins`에 INSERT한다. **참석 의사는 조건이 아니다.** 카톡에 참석을 체크했든 안 했든
판정에 영향을 주지 않는다 (§11).

> §5의 2축 4상태 표와 혼동하지 않는다. §5는 **버튼의 UI 상태**를 시간축×위치축으로
> 표현한 것이고, 여기 세 조건은 **서버가 기록을 남길지 판정하는 기준**이다.
> 클라이언트 게이트는 버튼을 켜고 끄기 위한 것일 뿐, 성립 여부는 서버가 다시 판단한다 (§4).

### 읽기와 쓰기의 경로가 다르다

| | 경로 | 이유 |
|---|---|---|
| **읽기** (명단, 세션, 내 기록) | 클라이언트 → Supabase 직접 + RLS | 조작해도 손해 없음. Realtime을 공짜로 얻음 |
| **쓰기** (출첵, 세션·회원 관리) | 클라이언트 → Server Action → service_role | 검증이 필요 |

`check_ins`의 읽기는 예외다. T46 이후 클라이언트 직접 조회에서 서버 조회로 바뀐다 (§17).

`check_ins`, `sessions`, `profiles`에 클라이언트용 INSERT/UPDATE/DELETE 정책을
**만들지 않는다.** RLS 기본 거부에 걸려 클라이언트는 절대 쓸 수 없다.
secret 키(`SUPABASE_SECRET_KEY`, `sb_secret_...`)는 서버 환경변수에만 두고
절대 클라이언트 번들에 넣지 않는다. 이 키는 Postgres의 `service_role` 롤로
인증되어 RLS를 전부 우회하므로, 노출되면 RLS 설계가 통째로 무의미해진다.

### RLS 자동 활성화 트리거 (`ensure_rls`)

`public`에 `ensure_rls` 이벤트 트리거가 걸려 있다. `ddl_command_end`마다 돌면서
새로 만들어진 `public` 테이블에 `enable row level security`를 건다.

**Supabase의 기본값이 아니다.** 대시보드의 옵트인 기능
(Authentication > Auto-enable RLS for new tables)을 이 프로젝트에서 켜서 생긴 것이다.
정의는 `supabase/migrations/0003_event_trigger_ensure_rls.sql`에 그대로 옮겨 뒀다.

- **이 트리거는 정책을 만들지 않는다.** RLS를 켜기만 한다
- **실제 통제 수단은 테이블을 새로 만드는 마이그레이션이 명시적으로 적는
  `enable row level security`다.** 이 트리거는 그 위에 덧대는 안전망일 뿐이다.
  **테이블을 만드는** 마이그레이션에서 그 문장을 빼지 않는다 —
  트리거가 꺼졌거나 없는 프로젝트에서 테이블이 그대로 열린다
- **이미 RLS가 켜진 테이블에 컬럼·인덱스를 더하는 마이그레이션은 다시 적지 않는다.**
  RLS 활성화의 정보원이 둘로 갈린다. 0004가 이 경우다

---

## 4. 데이터 흐름

```
브라우저
  ① Geolocation watchPosition → { lat, lng, accuracy }
  ② 카카오맵에 반경 원 · 내 위치 · 오차 원 렌더
  ③ haversine 거리 계산 (클라이언트)
     → 버튼 활성 여부 결정. 이건 UI용일 뿐이다
  ④ 출첵 버튼 클릭 → { sessionId, lat, lng, accuracy } 전송
        ↓
Next.js Server Action (Vercel)
  ⑤ 세션 조회 · 서버 시각으로 창 검증
  ⑥ 거리 재계산 · 회원 자격 · 중복 검사
        ↓
Supabase Postgres
  ⑦ INSERT check_ins (checked_at = now())
  ⑧ Realtime 브로드캐스트
        ↓
모두의 브라우저 → 명단 갱신
```

③의 계산 결과는 버튼을 켜고 끄기 위한 것일 뿐, 기록 여부는 ⑤⑥에서 서버가 다시 판단한다.

---

## 5. 출첵 게이트 — 2축 4상태

버튼 상태는 **시간축 × 위치축**으로 결정된다. UI에서 가장 중요한 부분이다.

| | 반경 밖 | 반경 안 |
|---|---|---|
| **창 이전** | 비활성 · "06:40부터 · 12분 남음" | 비활성 · "도착 확인 · 06:40에 열립니다" |
| **창 안** | 비활성 · "**82m** 더 가까이 가세요" | **활성 · "집합하기"** |
| **창 이후** | 비활성 · "출첵 마감" | 비활성 · "마감 · 운영자에게 문의" |

**비활성 이유가 항상 버튼 바로 위에 한 줄로 떠야 한다.**
"왜 안 눌리지"가 이 앱의 유일한 실패 모드다.

### 정확도 처리

`accuracy > radius`이면 판정은 그대로 두되 경고만 띄운다.

> GPS 오차(±Nm)가 반경보다 큽니다. 하늘이 트인 곳에서 잠시 기다려 주세요.

**정확도를 이유로 출첵을 막지 않는다.** 실제로 도착한 사람을 막는 것이 오차보다 큰 문제다.

### 창 밖 체크인

창 종료는 **`meet_at + 10분`** 으로 고정한다. `open_after_min`은 10에서 움직이지 않고,
운영자도 바꾸지 않는다 (§11).

**창 안이면 출석, 창 밖이면 거부.** 그 사이의 상태를 두지 않는다.
창 밖에서 누르면 게이트가 막고 **`check_ins` 행이 생기지 않는다.**

**"지각"이라는 상태를 만들지 않는다.** 표시할 때 계산하지도, 집계할 때 거르지도 않는다.
판정이 게이트 한 곳에만 있어야 화면과 집계의 기준이 갈라지지 않는다.

---

## 6. 회원 · 권한

**가입 신청 → 운영자 승인** 방식.

```
카카오 로그인 → profiles 자동 생성 (status='pending')
                      ↓
              승인 대기 화면
                      ↓
        운영자 승인 → status='active' → 앱 사용 가능
```

| status | 의미 | 행선지 |
|---|---|---|
| `pending` | 신청함. 승인 대기 화면만 볼 수 있음 | `/pending` |
| `active` | 정회원. 출첵 가능 | 통과 |
| `rejected` | 거절됨 | `/pending` (문구만 다름) |
| `inactive` | 탈퇴·휴면. 출석률 통계에서 제외 | `/pending` (문구만 다름) |

profiles 행이 없으면 pending으로 취급한다(fail-closed).

| role | 권한 |
|---|---|
| `member` | 출첵, 명단 조회 |
| `admin` | 회원 승인, 장소·일정 관리, 수동 출첵 |

- `member` / `admin` 두 값. **`admin`만 `/admin/*`에 진입할 수 있다** (`proxy.ts` 가드).
- 초기 운영자 1명은 Supabase 대시보드에서 `status='active'`, `role='admin'`으로 수동 지정한다
  (`SCHEMA.sql` 말미의 UPDATE 문).
- 이후 운영자 추가·해제 UI는 T26 이후의 몫이며 T14 범위가 아니다.
- role 가드도 §15와 같이 화면 전환용이고, 최종 인가는 Server Action이 한다 (§17).

카카오에서 `account_email` 동의 항목은 비즈 앱 전환이 필요하므로 **이메일에 의존하지 않는다.**
닉네임만으로 굴러가야 한다.

### 출석률

**출석률의 분모는 해당 기간의 전체 세션 수로 한다.**
참석 의사를 기록하지 않으므로 "올 예정이었던 세션"이라는 분모는 존재하지 않는다 (§11).

**번개 세션은 분자에만 들어간다. 분모에는 넣지 않는다** (§17).
번개에 나오면 출석 1회로 인정되지만, 번개가 열렸다는 사실만으로 전체 세션 수가 늘지는
않는다. 번개를 몰랐던 회원의 출석률이 함께 떨어지면 안 되기 때문이다.

그 결과 출석률이 100%를 넘을 수 있다. **표시할 때 `min(1.0)`으로 캡하고,
번개 참석 횟수를 별도 지표로 병기한다.** 캡을 씌운 뒤에도 "번개 3회"가 따로 보여야
번개에 열심히 나온 사람의 기여가 사라지지 않는다.

---

## 7. 세션

**반복 일정 + 임시 세션** 둘 다 지원한다.

- `recurring_schedules` — 요일·시각 규칙 (예: 매주 화/목/토 06:50)
- `sessions` — 실제 집합 하나. 반복 규칙에서 자동 생성되거나 운영자가 임시로 만든다

세션 유형(정규 `regular` / 번개 `flash`)과 번개 개설·취소 정책은 §17.

세션은 **매일 한 번 도는 크론이 향후 14일치를 미리 생성**한다.
이 크론이 Supabase 무료 티어의 7일 무활동 정지도 함께 막는다 (일석이조).

세션에는 장소의 **좌표·반경·이름을 스냅샷으로 복사**한다.
장소를 나중에 옮기거나 반경을 바꿔도 과거 기록의 판정 근거가 틀어지지 않아야 한다.

반경은 `places`가 아니라 `sessions`에 둔다. 같은 장소라도 계절·날씨에 따라 GPS 오차가 달라진다.

세션 창 경계(opens_at/closes_at)는 DB에 저장하지 않는다.
판정은 `lib/window.ts`가 하고, DB는 `meet_at`과 ±분만 갖는다.

---

## 8. 프라이버시

**미도착자의 실시간 위치는 수집하지도, 저장하지도, 표시하지도 않는다.**

저장하는 위치 정보는 `check_ins.dist_m`(출첵 순간의 거리)과 `accuracy_m` 단 둘뿐이다.
원좌표(lat/lng)는 서버 검증에만 쓰고 **저장하지 않는다.**

운영자 화면에도 "오는 중인 사람" 같은 기능은 만들지 않는다. 요청이 와도 거절한다.

---

## 9. 지도와 레이더

지도가 기본 화면이지만, **레이더(SVG)를 폴백으로 유지한다.**

카카오맵은 타일을 네트워크로 받아온다. 아침 7시 야외에서 신호가 나쁘면 화면이 회색이 되고,
출첵이 불가능해 보인다. 레이더는 순수 SVG라 좌표만 있으면 무조건 그려진다.

- 지도 위에 **거리 숫자와 출첵 버튼을 오버레이**한다. 지도 아래에 두면 스크롤해야 하고, 7시에 그건 실패다
- 타일 로딩 실패 시 자동으로 레이더로 전환
- "길찾기" 버튼은 카카오맵 앱 딥링크 (SDK 불필요). MobileWeb 스킴(m.map.kakao.com/scheme/route), 출발지 생략, 도보 고정

---

## 10. UI 원칙

- 모든 문구는 한국어. 존댓말, 짧게
- 다크모드 대응
- **아침 7시에 밖에서 장갑 낀 손으로 쓰는 앱이다.** 버튼 크게, 글자 굵게, 탭 영역 넉넉하게
- 색은 두 축만: **대기(반경 밖)** 와 **활성(반경 안)**. 이 구분이 화면에서 가장 먼저 읽혀야 한다
- 사용자 메인은 스크롤 없는 한 화면

---

## 11. 하지 않을 것

| 금지 | 이유 |
|---|---|
| 백그라운드 위치 · 지오펜싱 | 웹앱이라 불가능하고, 애초에 불필요 |
| 이메일/비밀번호 가입 | 신입이 가입하다 이탈. 카카오 로그인만 |
| 미도착자 위치 추적 | 프라이버시. §8 |
| 위도·경도 직접 입력 UI | 현재 위치 지정 / 지도 핀 / 저장된 장소로 대체. 디버그 서랍에만 남김 |
| 클라이언트에서 DB 직접 쓰기 | RLS 기본 거부. §3 |
| 정확도를 이유로 출첵 차단 | §5 |
| 지각 상태를 두는 것 | 창 안=출석, 창 밖=거부. 중간 상태 없음. §5 |
| 창 길이 조정 UI | 창 종료는 10분 고정. §5 |
| 카카오톡 이벤트·투표 결과 가져오기 | 카톡은 사전 의사 확인, 앱은 사후 사실 기록. 아래 |
| 번개 개설자에게 참석자 명단 노출 | 회원 명단이 사실상 공개 정보라 차집합으로 미참석자를 역산할 수 있다. §17 |
| 번개 사전 승인 절차 | 즉시성이 기능의 핵심. 사후 통제로 대체한다. §17 |

### 카카오톡과 앱은 동기화하지 않는다

카카오톡 이벤트·투표 결과를 앱으로 가져오지 않는다. 두 시스템은 동기화하지 않는다.
**카톡은 사전 의사 확인, 앱은 사후 사실 기록이다.**

1. **참석 의사를 게이트로 쓰면 실제로 온 사람을 막는다.** 카톡에 체크하지 않고 나온 사람이
   현장에 서 있는데 버튼이 안 눌린다. §3·§5의 판단과 같은 이유 — 실제로 도착한 사람을
   막는 것이 이 앱의 가장 큰 실패다
2. **자동화가 불가능하다.** 카카오는 채팅방 투표·일정의 참·불참을 외부에서 조회하는 API를
   제공하지 않는다
3. **매 세션 수작업이 필요하다.** 카톡 닉네임과 앱 계정을 사람이 일일이 맞춰야 한다

단방향 내보내기(앱 명단 → 카톡 붙여넣기)는 예외다. TASKS.md T36.

---

## 12. 환경변수

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=   # sb_publishable_... 브라우저 노출 전제
SUPABASE_SECRET_KEY=                    # sb_secret_... 서버 전용. 절대 클라이언트 번들 금지
NEXT_PUBLIC_KAKAO_MAP_KEY=              # 카카오맵 JavaScript 키(브라우저 노출 공개값)
CRON_SECRET=                            # 직접 정해 Vercel에 등록. 아래 주의
GOOGLE_SERVICE_ACCOUNT_JSON_BASE64=     # 서비스 계정 JSON 전체를 base64. 서버 전용
GOOGLE_SHEET_ID=                        # 스프레드시트 URL의 /d/ 와 /edit 사이. 서버 전용
```

**2025-11-01 이후 만든 Supabase 프로젝트에는 legacy `anon` / `service_role` 키가
발급되지 않는다.** 대시보드에 나오는 것은 publishable / secret 두 가지뿐이고,
`sb_publishable_`가 옛 anon 키를, `sb_secret_`가 옛 service_role 키를 대신한다.
권한 모델은 그대로다 — secret 키는 여전히 `service_role` 롤로 인증되어 RLS를 우회한다 (§3).

`CRON_SECRET`은 **Vercel이 만들어 주지 않는다.** 사람이 값을 정해 Vercel 환경변수에
등록하면, Vercel이 크론 요청에 `Authorization: Bearer <값>`으로 실어 보낼 뿐이다.
등록하지 않으면 헤더가 오지 않아 T32의 검증이 항상 실패한다.

카카오 개발자 콘솔에 **플랫폼 도메인과 Redirect URI 양쪽 모두** 등록해야 한다.
하나만 빠져도 로그인이 실패한다.

로그인용 **REST API 키와 Client Secret은 env에 넣지 않는다.** Supabase 대시보드
Auth > Providers > Kakao에만 저장한다 — 코드 교환은 Supabase 서버가 하고, 앱은
이 값을 볼 일이 없다.

---

## 13. 폴더 구조

```
app/
  (auth)/login/            카카오 로그인
  (auth)/pending/          승인 대기
  auth/callback/           OAuth 콜백
  (app)/page.tsx           사용자 메인 (출첵)
  (app)/me/                내 기록
  admin/members/           회원 승인
  admin/places/            장소 관리
  admin/schedules/         반복 일정
  admin/sessions/new/      임시 세션 생성 (T29)
  admin/sessions/[id]/     세션 현황 · 수동 출첵
  api/cron/daily/          세션 생성 + keep-alive
lib/
  geo.ts                   haversine · bearing
  window.ts                세션 창 판정
  gate.ts                  출첵 게이트 (2축 4상태)
  supabase/                client · server · admin
components/
  Radar.tsx                SVG 레이더 (순수 컴포넌트)
  KakaoMap.tsx             지도 + 반경 원 + 오차 원
  CheckInButton.tsx
actions/
  check-in.ts              Server Action
  places.ts                장소 등록·활성 토글 Server Action (T27)
  create-session.ts        임시 세션 생성 Server Action (T29)
proxy.ts                   status·role별 접근 제어 (T13, T14)
supabase/migrations/
```

`proxy.ts`는 **프로젝트 루트에 `app/`과 같은 층**에 둔다. Next.js 16에서 미들웨어가 이 이름으로 바뀌었고, 프로젝트당 한 개만 허용된다 (§15).

`lib/geo.ts`, `lib/window.ts`, `lib/gate.ts`는 **순수 함수만** 담는다.
React·Supabase·브라우저 API에 의존하지 않아야 테스트가 쉽다.

---

## 14. 미결 사항

작업 중 아래에 부딪히면 임의로 정하지 말고 물어본다.

- 세션 취소 시 이미 한 출첵을 어떻게 처리하는가
- 회원 탈퇴 시 과거 출첵 기록을 남기는가
- `anon`·`authenticated`에 남아 있는 테이블 write GRANT를 revoke하는가
  (지금은 RLS가 유일한 방어선이다. 관찰 근거는 `TASKS.md` T04 메모)
- `open_after_min`을 DB CHECK로 잠글지 — 다음 스키마 정리 마이그레이션에서 판단
  (지금은 §5의 정책으로만 10분에 묶여 있고, 컬럼은 default 10 / check 0~120 그대로다)

---

## 15. Next.js 16 주의사항

학습 데이터에 있는 Next.js와 다르다. 코드를 쓰기 전에
`node_modules/next/dist/docs/`의 해당 가이드를 읽는다 (`AGENTS.md`가 지시하는 워크플로).

### 미들웨어가 `proxy.ts`로 바뀌었다

`middleware.ts`가 아니라 **`proxy.ts`**다. 이름만 바뀌었고 기능은 같다.
프로젝트 루트, `app/`과 같은 층에 두며 **프로젝트당 한 개만** 지원된다.
로직을 나누고 싶으면 별도 파일로 빼서 `proxy.ts`가 import한다.

기본 export 또는 이름있는 `proxy` export 둘 다 가능하다.

> 인가(authorization)를 여기서 끝내지 않는다. 문서가 명시적으로 경고한다 —
> `proxy.ts`는 낙관적 검사(optimistic check)용이고, 세션 관리나 권한 판정의
> 최종 방어선이 아니다. T13·T14의 가드는 화면 전환용이고, 실제 방어는
> RLS(§3)와 Server Action(§4)이 한다.

### `params` · `searchParams` · `cookies()`가 async다

전부 Promise를 반환한다. `await` 없이 쓰면 조용히 깨진다.

```ts
export default async function Page({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  const { id } = await params
}

const cookieStore = await cookies()
```

Supabase 서버 클라이언트(T11)가 쿠키를 다루므로 **T11에서 가장 먼저 부딪힌다.**

### Turbopack이 기본 번들러다

`next dev`·`next build` 모두 Turbopack을 쓴다. `--turbopack` 플래그는 이제 불필요하다.
webpack으로 되돌리려면 `next dev --webpack` / `next build --webpack`.

### 런타임 요구사항

| 항목 | 최소 |
|---|---|
| Node.js | 20.9.0 (Active LTS). 18은 지원 종료 |
| TypeScript | 5.1.0 |
| 브라우저 | Chrome/Edge/Firefox 111+, Safari 16.4+ |

브라우저 하한이 이 앱에 실제로 걸린다 — 아침 집합에 오래된 폰을 들고 오는 사람이 있으면
Geolocation 이전에 페이지 자체가 뜨지 않을 수 있다. V03에서 확인할 것.

### `AGENTS.md`는 자동 생성 파일이다

`next dev`가 실행될 때마다 `<!-- BEGIN:nextjs-agent-rules -->` 블록을 다시 써넣는다
(`node_modules/next/dist/server/lib/generate-agent-files.js`).
지워도 되돌아오므로 **커밋해서 워킹 트리를 깨끗하게 유지한다.** `.gitignore`에 넣지 않는다.

---

## 16. 구글 시트 동기화

### 목적

출석 데이터를 구글 시트에 자동 반영해 엑셀에서 바로 열람·가공할 수 있게 한다.

구글 시트를 선택한 이유: OneDrive의 실제 `.xlsx`에 직접 쓰려면 Microsoft Graph와
Azure 앱 등록이 필요해 동아리 규모에 과하다. 구글 시트는 언제든 `.xlsx`로 내려받거나
엑셀에서 열 수 있어 요구사항을 충족한다.

### 동기화 방식 — 행 추가(append)가 아니라 월별 탭 전체 재작성

시트 탭을 월 단위로 분리한다 (예: `2026-09`). 체크인 또는 출석 기록 수정이 발생하면
해당 월 탭의 내용을 DB에서 다시 읽어 통째로 덮어쓴다 (`values.clear` 후 `values.update`).

월별 탭에 **`세션 유형` 열**을 추가한다 (정규 / 번개, §17).
**정규와 번개를 탭으로 분리하지 않는다.** 월별 전체 재작성 방식의 idempotency 관리
대상을 늘리지 않기 위해서다 — 탭이 둘로 갈리면 한쪽만 덮어써진 중간 상태가 생긴다.

**append를 쓰지 않는 이유**

- 출석 수정·취소 시 시트에 옛 행이 남는다
- 재시도가 겹치면 중복 행이 생긴다

**전체 재작성의 이점**

- **멱등** — 몇 번 실행해도 결과가 같다
- 수정·취소가 자동 반영된다. 특정 행을 찾아 지우는 로직이 불필요하다
- **자가 복구** — 한 번 실패해도 다음 체크인 때 전체가 다시 쓰이며 저절로 맞춰진다

한 달치 최대 규모는 약 12세션 × 40명 = 500행 수준이라 일괄 쓰기에 부담이 없다.

### 실패 격리 원칙

시트 쓰기는 체크인 트랜잭션에 포함하지 않는다.

```
(1) DB 기록 확정  →  (2) after()로 응답 이후 시트 동기화 실행
```

동기화 실패는 로그만 남기고 삼킨다. 위로 던지지 않는다.
**구글 API 장애로 부원의 출석이 실패해서는 안 된다.**

자동 동기화가 실패했을 때의 탈출구로 관리자 수동 동기화 버튼을 둔다 (T41).

### 쿼터

Sheets API 쓰기 한도는 **프로젝트당 분당 300건, 사용자당 분당 60건**이다.
서비스 계정의 모든 호출은 한 명의 사용자로 계산되므로 **실질 한도는 분당 60건**이다.

429가 발생하면 지수 백오프로 1~2회만 재시도한다. 어차피 다음 이벤트가 복구한다.

### 구현 경계

- 구글 API 접촉면은 `lib/sheets.ts` 한 곳에 가둔다
  (세션 창 판단을 `lib/window.ts`에 몰아둔 것과 같은 원칙)
- `googleapis` 전체 패키지 대신 `google-auth-library`로 토큰만 발급받고
  REST 엔드포인트에 `fetch`로 호출한다. 서버리스 번들 크기 때문이다
- 스코프는 `https://www.googleapis.com/auth/spreadsheets` 하나

### 개인정보 범위

- 시트에도 GPS 원좌표는 넣지 않는다. 거리(m)와 정확도(m)만 기록한다 (§8 원칙 유지)
- 시트에는 미도착자를 넣지 않는다. `check_ins`에 기록된 출석자만 들어간다
  (§8 · T31과 같은 이유 — 앱이 결석을 선고하지 않는다)
- **시트 공유 범위가 곧 개인정보 유출 범위다.** DB에 RLS를 걸어둔 의미가 사라지지
  않도록, 시트는 "링크가 있는 모든 사용자"로 열지 않는다.
  서비스 계정만 편집자, 임원 계정만 뷰어로 추가한다

### 운영 규칙

**앱이 쓰는 탭은 사람이 손대지 않는다.** 앱이 탭 전체를 덮어쓰므로 그 탭에 적은
메모나 수식은 다음 체크인 때 사라진다.

- 수식·피벗은 반드시 별도 탭에서 참조만 한다
- 해당 탭 1행에 이 경고를 문구로 넣는다

### 환경변수

```
GOOGLE_SERVICE_ACCOUNT_JSON_BASE64=   # 서비스 계정 JSON 키 전체를 base64 인코딩한 값
GOOGLE_SHEET_ID=                      # 스프레드시트 URL의 /d/ 와 /edit 사이 문자열
```

개인키의 줄바꿈이 환경변수에서 깨지는 문제를 피하기 위해 JSON 키를 base64로 저장한다.

**둘 다 `NEXT_PUBLIC_` 접두사를 붙이지 않는다.** 붙이면 브라우저 번들에 노출된다.

---

## 17. 세션 유형 — 정규와 번개

정해진 시각의 정규 집합 외에, 회원이 즉석에서 여는 **번개러닝**을 지원한다.

### 유형

| `session_type` | 의미 | 개설 권한 |
|---|---|---|
| `regular` | 정규 집합. 반복 일정에서 생성되거나 운영자가 임시로 만든다 | 운영자만 |
| `flash` | 번개. 회원이 즉석에서 연다 | 승인된 멤버(`status='active'`) 전원 + 운영자 |

**임시 세션(T29)과 번개는 다르다.** 둘 다 `schedule_id`가 null이지만, 임시 세션은
운영자가 만든 `regular`이고 번개는 회원이 만든 `flash`다.
**둘을 가르는 것은 `schedule_id`가 아니라 `session_type`이다.**

### 개설 정책 — 사전 승인 없음

**번개는 사전 승인을 거치지 않는다. 개설 즉시 공개된다.**
승인을 기다려야 하면 "지금 나가실 분"이라는 기능 자체가 성립하지 않는다.
즉시성이 이 기능의 핵심이다.

대신 **사후 통제**로 대체한다.

- 운영자는 임의의 세션을 삭제할 수 있다
- **1인당 동시 오픈 가능한 번개는 1개**

여기서 **"동시 오픈"은 `canceled_at is null`이고 창 마감(`meet_at + open_after_min`)이
아직 지나지 않은 `flash` 세션**을 말한다. 창 판정 기준은 `lib/window.ts`(T08)와 같다.

- **위치**: 개설자가 카카오맵에서 좌표를 지정한다. §11의 "위도·경도 직접 입력 UI 금지"에
  걸리지 않는다 — 지도 핀은 처음부터 허용된 방식이다
- **반경**: 서버 상수로 고정한다. **클라이언트가 보낸 반경 값은 무시한다.**
  회원이 반경을 정하게 두면 500m짜리 번개로 출첵 판정이 무의미해진다.
  상수의 실제 값은 여기서 정하지 않는다 — V01 측정 결과를 보고 T43에서 정한다 (§7과 같은 이유)
- **시작 시각**: 현재 시각 이후만 허용한다

### 수정 · 취소 · 삭제

| 행위 | 권한 | 처리 |
|---|---|---|
| 수정 | 개설자 본인 또는 운영자 | — |
| 취소 | 개설자 본인 또는 운영자 | `canceled_at` · `cancel_reason`을 채운다 |
| 하드 삭제 | **운영자만** | `check_ins`가 하나라도 있으면 금지 |

**취소에 `status` 컬럼을 새로 만들지 않는다.** 취소는 이미 `canceled_at`/`cancel_reason`으로
모델링되어 있고, §3 검증표의 "세션 유효성 · 미취소"가 이것을 가리킨다.
같은 사실을 두 곳에 저장하면 반드시 어긋난다.

**하드 삭제를 기본 취소 수단으로 쓰지 않는 이유**: 기존 체크인 레코드가 고아가 된다.
취소는 "열렸다가 무산된 집합"이라는 사실을 남기지만, 삭제는 그 사실 자체를 지운다.

### 명단 비노출

| 보는 사람 | 보이는 것 |
|---|---|
| 일반 회원 | 세션 정보 + 참석 **인원 수** + 본인의 체크인 상태 |
| 운영자 | 위 + 참석자 명단 |

- 미참석자 명단은 앱·시트 어디에도 존재하지 않는다 (§8 기존 원칙 유지)
- 참석자 명단은 **운영자만** 조회 가능하다

**번개 개설자에게 예외를 두지 않는다.** 개설자도 일반 회원과 똑같이 인원 수만 본다.
동아리 회원 명단은 사실상 공개 정보이므로, 참석자 명단을 보여주면
**미참석자를 차집합으로 역산할 수 있다.** "내 번개니까 누가 왔는지는 봐야 하지 않나"는
합리적으로 들리지만, 그 예외 하나가 §8의 미참석자 비노출 원칙을 통째로 무력화한다.

### 구현 제약

**명단 비노출을 UI 조건부 렌더링으로 구현하지 않는다.**
응답 payload 자체에 명단이 들어가지 않도록 조회 함수를 분리한다.

```
getSessionSummary(sessionId)    → 세션 정보 + 인원 수 + 내 체크인 상태
getSessionAttendees(sessionId)  → 참석자 명단. 운영자 전용
```

**함수 분리만으로는 부족하다.** 클라이언트는 publishable 키로 `check_ins`를 직접
SELECT할 수 있다 (§3의 읽기 경로). RLS `check_ins_read` 정책을 본인 행 + 운영자로
좁혀야 실제로 막힌다 (T46). 정책을 좁히면 T25의 Realtime 명단이 함께 닫히므로,
명단 스트림을 인원 수 스트림으로 재설계한다 (T47).

**동시 오픈 1개 제한은 DB partial unique index로 구현할 수 없다.**
종료 여부 판단이 `now()` 비교라 IMMUTABLE 조건이 아니다.
`opens_at`/`closes_at` 생성 열을 두지 않은 것과 정확히 같은 이유다 (§7).
Server Action 층에서 검증한다.

**Server Action은 직접 POST로 도달 가능한 공개 엔드포인트다.**
지금까지 쓰기는 운영자만 했지만, 번개부터는 일반 회원에게 쓰기가 열린다.
`proxy.ts`의 가드는 화면 전환용이고 방어선이 아니다 (§15).

- 모든 액션 내부에서 인증·인가를 **직접** 검증한다
- 클라이언트가 보낸 `created_by`를 신뢰하지 않는다. 서버 세션에서 재조회해 비교한다

---

## 18. 지도 실패 시 레이더 폴백 (T19)

`components/MapOrRadar.tsx`. §9의 "타일 로딩 실패 시 자동으로 레이더로 전환"을 구현한 것이다.
아침 7시 야외에서 신호가 나쁘면 지도가 회색으로 남고 출첵이 불가능해 보인다. 레이더는 순수
SVG라 좌표만 있으면 무조건 그려진다. 결정은 네 가지다.

### 폴백 상태는 KakaoMap 밖(MapOrRadar)이 소유한다

`KakaoMap`은 그리기만 하고, 실패는 `onError(reason)` 콜백 하나로만 알린다. `reason`은 셋이다.

| reason | 뜻 |
|---|---|
| `script` | `sdk.js` 태그가 `error` 이벤트를 냈다 (네트워크 · 차단) |
| `timeout` | `timeoutMs` 안에 SDK `load` 콜백에 도달하지 못했다 |
| `sdk` | 태그는 로드됐는데 `window.kakao.maps`가 없다 (차단 확장이 빈 응답을 돌려줬거나 키가 거부됨) |

`KakaoMap`은 `Radar`의 존재를 모른다. 어느 쪽을 보여줄지(`mapFailed`)는 `MapOrRadar`가 들고
있고, 그 부모가 `lib/geo.ts`로 계산한 거리·방위를 넘긴다. 지도 컴포넌트에 폴백을 넣으면
"지도"가 "지도 또는 레이더"가 되어 T18의 경계(§5 — 판정은 `lib/gate.ts` 한 곳)가 흐려진다.

`onError`는 인스턴스당 한 번만 불린다. script 에러와 타임아웃이 겹쳐 두 번 오면 부모가 폴백
이유를 두 번 갱신하고, 리마운트 뒤 늦게 온 콜백이 새 인스턴스를 오염시킨다.

**지도 키 미설정은 세 사유에 넣지 않는다.** 배포 설정 오류라 레이더로 넘겨 가릴 일이 아니고,
개발자가 바로 봐야 하므로 `KakaoMap`이 "지도 키가 설정되지 않았습니다"를 직접 띄운다.

### 타임아웃은 8초, 실패한 script 태그는 제거한다

`timeoutMs` 기본값은 8000. 타임아웃과 `error` 양쪽 모두에서 **`<script>` 태그를 DOM에서 지운다.**
실패한 태그가 남아 있으면 다음 인스턴스가 그 태그를 재사용해 `error` 이벤트를 다시 받지 못하고
로딩 상태에 갇히거나, "지도 다시 시도"가 그 태그에 리스너만 다시 붙여 또 8초를 기다린다.

**언마운트에서는 태그를 지우지 않는다.** 다시 붙이면 SDK가 두 번 실행되어 전역이 깨지고, 같은
페이지의 다른 인스턴스가 쓰고 있을 수 있다. 제거는 로드 실패·타임아웃 때만 한다.

### "지도 다시 시도"는 리마운트, 자동 재시도는 없다

폴백 화면의 버튼은 `key`를 바꿔 `KakaoMap`을 새로 마운트한다. 새 인스턴스는 지워진 태그 대신
새 태그로 처음부터 다시 시도한다. **자동 재시도는 두지 않는다** — 체크인 도중 지도가 갑자기
돌아오면 레이아웃이 튀어 버튼을 잘못 누른다. 폴백 박스는 `KakaoMap`과 같은 크기를 유지해
전환 시에도 아래 버튼이 움직이지 않는다.

### 폴백은 Radar를 그리고, 게이트는 영향받지 않는다

폴백 시 `components/Radar.tsx`(T16)를 같은 자리에 그린다. 거리·방위는 부모가 `lib/geo.ts`의
`distanceM`·`bearingDeg`로 계산한 값 그대로다 — 지도일 때와 레이더일 때 다른 계산을 쓰지 않는다.
위치가 아직 없으면 "위치를 기다리는 중입니다"만 띄운다.

출석 게이트는 이 컴포넌트 밖에 있다. 지도가 실패해 레이더로 바뀌어도 버튼은 거리로만 열리고
닫힌다. `/dev`의 `DevMap`이 이 케이스를 눈으로 확인하는 자리다 (`app/dev/DevMap.tsx`).
폴백 이유(`script` 등)는 개발 확인용으로 작게 병기하고, 사용자 문구는
"지도를 불러오지 못했어요. 레이더로 표시합니다." 하나다.

---

## 19. 체크인 Server Action (T21)

`actions/check-in.ts`. 출첵의 유일한 쓰기 경로이며, §3의 세 조건(승인된 회원 · 창 안 · 반경 안)을
서버가 전부 다시 확인한 뒤에만 `check_ins`에 INSERT한다. 결정은 네 가지다.

### 중복 출첵은 DB UNIQUE가 막고, 23505는 성공으로 돌려준다

`check_ins`의 `unique (session_id, member_id)`(0001)가 두 번째 출첵을 막는다.
INSERT가 `23505`(unique_violation)로 실패하면 `{ ok: true, already: true }`를 돌려준다 —
부원 입장에서는 출석이 되어 있는 상태이지 실패가 아니다.

**INSERT 전에 SELECT로 중복을 검사하지 않는다.** 같은 부원의 요청 두 개가 겹치면 둘 다 SELECT를
통과하고, 결국 판정은 UNIQUE가 한다. 그 외 에러 코드는 전부 `db_error`이고, 원문은 서버 로그까지만
남긴다. 클라이언트에는 코드도 원문도 보내지 않는다 (`lib/checkInResult.ts` `mapInsertError`).

### 좌표는 서버 재계산에만 쓰고 저장·로깅하지 않는다

클라이언트가 보낸 `lat/lng`는 `lib/geo.ts`로 거리를 다시 계산하는 입력일 뿐이다 (§3).
저장하는 것은 `dist_m`·`accuracy_m`(둘 다 정수로 반올림)뿐이고, `checked_at`은 DB default
`now()`에 맡긴다 — 클라이언트 시각은 받지도 않는다 (§8, CLAUDE.md 절대 규칙).
`console.error`에도 좌표를 넣지 않는다.

`accuracy`는 기록만 하고 판정에 쓰지 않는다 (§5). 경고는 클라이언트(T10)의 몫이다.

### 시트 동기화 `after()`는 T40에서 붙인다

이 액션에는 `after()`도 `revalidatePath`도 없다. 시트 동기화 훅 연결은 T40(선행 T38 래퍼 · T39
재작성 로직), 리더보드 갱신은 T55의 몫이다. DB 기록이 확정된 뒤 응답 이후에 붙는 것들이라
(§16 실패 격리) 액션의 성공·실패 판정과 섞이지 않는다.

### 인증은 server.ts, INSERT만 admin.ts, 회원 자격은 액션 안에서 재확인

Server Action은 직접 POST로 도달 가능한 공개 엔드포인트다 (§17). 순서는 이렇다.

1. `lib/supabase/server.ts`(요청 쿠키 세션)로 `auth.getUser()`. 없으면 `unauthenticated`
2. 같은 클라이언트로 `profiles.status`를 읽어 `'active'`가 아니면 `not_active`.
   행이 없어도 `not_active`다 (fail-closed, `proxy.ts`와 같은 규칙). `proxy.ts`의 가드는
   화면 전환용이지 방어선이 아니므로 (§15) 여기서 다시 본다
3. `sessions`에서 `canceled_at is null`인 행만 조회. 없거나 취소됐으면 `no_session`.
   취소 세션에 별도 사유를 두지 않는다 — "출석할 세션이 없다"가 부원이 알아야 할 전부다
4. 서버 시각 `new Date()`로 `lib/window.ts` → `lib/gate.ts` 판정. 거부면 게이트의 `reason`·`message`를
   그대로 돌려준다. 창 밖이면 행이 생기지 않는다 (§5)
5. 여기까지 통과한 뒤에만 `lib/supabase/admin.ts`(service_role)로 INSERT

`getGate`가 요구하는 KST 시계 표기(`'06:40'`)는 `lib/kst.ts`가 만든다. Vercel 함수의 TZ는 UTC라
`Intl`·`toLocaleString`을 쓰면 문구가 환경에 끌려가므로, epoch ms + 9시간 산술로 고정한다.

---

## 20. 다음 세션 조회 (T22)

`lib/nextSession.ts`(순수) + `lib/queries/nextSession.ts`(server-only 조회).
메인 화면(T23)이 그릴 세션 하나를 고르고, 없으면 안내 문구를 만든다.

### "가장 가까운 세션"은 창이 안 닫힌 세션이다

`meet_at > now`로 고르면 창이 열려 있는 06:52에 06:50 세션이 사라진다.
그래서 `lib/window.ts`로 판정해 `after`만 뺀다. 창 판정을 SQL에 복제하지 않는다 —
조회는 `meet_at >= now − 120분`(open_after_min CHECK 상한)으로 후보만 좁히고, 판정은 JS가 한다.

### 겹치면 열린 세션 → 정규 → 이른 시각

번개(§17)는 정규 시간대에 겹칠 수 있고, 메인 화면은 세션 하나만 그린다.
창이 지금 열린 세션을 먼저 보고 그중 `regular`를 우선한다. 정규 장소에 서 있는 부원 화면에
번개가 뜨는 것을 막기 위해서다. 열린 세션이 없으면 열리기 전 세션 중 `meet_at`이 가장 이른 것.

### 세션이 없으면 반복 일정에서 다음 발생을 계산해 안내만 한다

"다음 집합: 화요일 06:50"은 `recurring_schedules`에서 계산한다. **표시 전용이다.**
세션 행이 없으면 체크인 액션이 `no_session`으로 거부하므로(§19) 보안 경계와 무관하다.
일정도 없으면 "예정된 집합이 없습니다". 조회 실패는 이 문구로 뭉개지 않고 별도 문구를 낸다.

### 조회는 server.ts, 결과는 세 갈래

RLS `sessions_read`를 그대로 타도록 `lib/supabase/server.ts`로 읽는다. admin 클라이언트는 쓰지 않는다.
결과 타입은 `session` / `notice` / `error` 셋이다. `lib/queries/`는 이 티켓에서 처음 생긴
서버 조회 모듈 폴더다.

---

## 21. 메인 화면 조립 (T23)

`app/(app)/page.tsx` + `components/MainScreen.tsx`. 사용자 메인 `/`의 뼈대다.
세션 헤더 / 지도 / 거리 숫자 / 상태 문구 / 버튼 자리 / 인원 수 자리를 375×667 한 화면에 넣는다.
**게이트 판정·체크인 호출·인원 수 조회는 여기 없다.** T24·T47이 채울 자리만 최종 크기로 잡는다.

### 조회는 서버, 그리기는 클라이언트

`page.tsx`는 async Server Component로 `getNextSession()`(§20)을 부르고 결과를 `MainScreen`에 props로
넘긴다. `/dev`의 `DevNextSession`과 같은 패턴이다. `MainScreen`은 `'use client'`이고
`NextSessionResult`를 **타입으로만** 가져온다 — `lib/queries/`는 server-only라 값 import는 막힌다.
`cookies()`를 타므로 `/`는 자동 dynamic이고 `export const dynamic`은 쓰지 않는다 (§15).

### 훅은 세션 분기 자식에서만 부른다

`useGeolocation`은 `SessionView` 안에서만 마운트된다. `notice`/`error`일 때는 지도 없이 중앙 문구
카드 하나뿐이고 GPS를 켤 이유가 없다. 훅 규칙상 조건부 호출이 안 되므로 분기를 컴포넌트로 나눈다.

### 100dvh 고정, 지도가 남는 높이를 전부 차지한다

`h-dvh`는 `page.tsx`의 `<main>` 한 곳에만 두고 `MainScreen`은 `h-full`이다. `dvh`는 부모 박스로
줄일 수 없어서, 이렇게 해야 `/dev`의 375×667 프레임(`DevMainScreen`)이 실제로 화면을 구속한다.
헤더 1줄 → 지도(`flex-1 min-h-0`) → 하단 1줄(`참석 인원 —` + 길찾기).

### 거리·버튼은 지도 위 오버레이, 상태 문구는 버튼 위 고정 높이

§9대로 거리 숫자(상단)와 버튼(하단)을 지도 위에 띄운다. 버튼은 56px 전체 폭이고 지금은 비활성
"준비 중"이다. 그 바로 위 상태 문구 영역은 `useGeolocation().message`와 `accuracyWarning`만 담고,
둘 다 없어도 `min-height`로 높이를 유지한다 — 문구가 떠도 버튼이 움직이면 안 된다.
색은 대기/활성 두 축뿐이고 (§10), 위치가 아직 없을 때(`--`)는 대기색이다.

### 하지 않는 것

`router.refresh` 자동 갱신 없음 — 창 경계 처리는 T24 게이트 몫이다. 좌표는 `SessionView` 안에서
거리·방위 계산과 지도에만 쓰고 화면·콘솔에 찍지 않는다 (§8).

---

## 22. 버튼 4상태 UI (T24)

`components/MainScreen.tsx`의 `SessionView`가 `lib/gate.ts` 결과로 "집합하기" 버튼을 켜고 끈다.
T24 시점에는 렌더만 했다. 클릭 연결은 §25(T57)이고, 이 절의 4상태 판정은 그대로다.

### 표시용 1초 시계와 서버 판정

`hooks/useNow.ts`는 `useSyncExternalStore`로 초 단위 내림 epoch ms를 구독한다. 같은 초 안에서는
스냅샷이 같아 리렌더가 없고, 렌더 중 `Date.now()`를 부르지 않는다. 서버 스냅샷은 고정값 0이다 —
서버 렌더에는 위치가 없어 게이트를 부르지 않으므로 화면에 드러나지 않는다.
**이 시계는 버튼을 켜고 끄기 위한 것이다.** 출첵 성립은 서버 게이트(T21, §19)가 서버 시각으로
다시 판정한다 (§3·§4). 창 경계에서 클라이언트 시계가 1초 어긋나도 서버가 거른다.

### `sim`은 dev 전용이다

`MainScreen`의 `sim?: { now?, distM? }`는 `/dev`의 `DevMainScreen`만 넘긴다 — 창 라디오(시간축) ×
거리 슬라이더(위치축)로 §5 표의 여섯 칸을 만든다. `app/(app)/page.tsx`는 넘기지 않는다.
훅은 조건 없이 부르고 주입값이 있으면 그걸 쓴다 (`const clock = useNow(); const now = sim?.now ?? clock`).
`sim.distM`이 있으면 방위는 0이고, 정확도 경고는 GPS 값 기준 그대로다 — 경고는 판정에 섞지 않는다 (§5).

### 라벨은 고정, 이유는 윗줄

버튼 라벨은 항상 "집합하기"다. 왜 안 눌리는지는 버튼 바로 위 상태 줄 1이 말한다 (§5):
위치가 없으면 GPS 안내 또는 "위치를 기다리는 중입니다", 게이트가 거부하면 `gate.message`,
켜져 있으면 빈 줄. 줄 높이는 항상 유지해 문구가 떠도 버튼이 움직이지 않는다.
색은 활성(반경 안)·대기 두 축뿐이다 (§10). 위치가 없으면 게이트를 부르지 않고 버튼은 비활성이다.
지도의 `inside`는 게이트가 아니라 `dist <= radius`다 — 창 이전이라도 지도색은 반경 기준이다.

---

## 23. 장소 등록 (T27)

`app/admin/places`(Server Component + `PlacesAdmin` 클라이언트 자식)와 `actions/places.ts`.
운영자가 현장에서 **"지금 내 위치를 집합 장소로 지정"** 버튼 하나로 `places` 행을 만들고,
목록에서 활성/비활성을 토글한다. 위도·경도 직접 입력 UI는 없다 (§11).

### P1 · 좌표는 버튼을 누르는 순간의 GPS 값 하나다

`useGeolocation()`이 들고 있는 최신 `position`을 그대로 폼에 담는다. 샘플 평균·재시도 없음 —
운영자는 같은 자리에 서 있고, 값이 마음에 안 들면 다시 누르면 된다. `position`이 없으면 버튼은
비활성이고 그 이유(권한·신호·대기)가 버튼 위에 뜬다.

**이 경로는 클라이언트 GPS 값을 저장 목적으로 받는 유일한 경로다.** `check_ins`는 좌표를 재계산에만
쓰고 버리지만 (§8·§19), `places.lat/lng`에는 남는다. 두 가지가 다르다.
(1) 운영자 본인이 "여기가 집합 장소"라고 지정한 값이지, §8이 금지하는 부원의 원좌표가 아니다.
(2) 출첵 게이트 판정과 무관하다 — 세션은 이 값을 스냅샷해서 쓰고 (§7), 판정은 T21이 한다.
`console.error`에는 여전히 좌표를 넣지 않는다.

`정확도 ±Nm`을 상시 표시한다. **이 숫자가 V01(반경 측정)의 관찰 수단이다** — 용봉탑 앞에서 몇 분간
서서 이 값을 보고 반경 기본값을 정한다 (TASKS.md V01).

### 정확도 경고는 저장을 막지 않는다

`lib/accuracy.ts`의 `accuracyWarning({ accuracy: 지정 시점의 정확도, radius: 슬라이더 값 })`이 문구를 주면
경고만 띄운다. §5와 같은 원칙이다 — 실제로 현장에 선 사람을 오차 때문에 막는 게 오차보다 큰 문제다.
운영자가 경고를 보고 반경을 올리거나 다시 지정하는 것은 운영자의 판단이다.

### P2 · 지도 핀은 T58로 미룬다

`components/KakaoMap.tsx`는 T18·T19에서 두 번 손댔다. 핀 찍기(`onPick`)를 붙이면 3차 수정이고,
T43·T44 번개 개설도 같은 핀 UI를 쓰므로 한 번에 설계하는 게 맞다. T27은 현재 위치 지정만으로
"현장에서 버튼 하나로 등록"이라는 완료 기준을 만족한다.

### P3 · 폼은 이름·반경·(버튼이 채우는) 좌표뿐

- 이름: `trim` 후 1~40자. 저장도 `trim`한 값.
- 반경: 슬라이더 20~200m, 5m 단위, 기본 60m. DB CHECK는 20~300이지만 **T28 반복 일정 슬라이더와 같은
  범위로 보수적으로 좁힌다.** 상수(`PLACE_RADIUS_*`)는 `lib/placeResult.ts`에 두고 슬라이더와 서버 검증이
  공유한다 — 슬라이더로 고른 값이 서버에서 거부되는 일이 없어야 한다.
- `components/DistanceSlider.tsx`는 재사용하지 않는다. -1=꺼짐이라는 의미가 다르고 프로덕션에서 `null`을
  반환하는 개발 도구다.

### P4 · 목록은 이름·반경·등록일·활성 여부만

좌표는 표시하지 않는다 (select도 하지 않는다). 등록일은 `lib/kst.ts` `kstDateString`으로 KST 날짜만.
행마다 `is_active` 토글 하나. **삭제·이름 수정·반경 수정 UI는 없다** — 잘못 찍었으면 비활성으로 두고
새로 등록한다. 세션이 `place_id`로 참조하므로 삭제는 애초에 위험하다.

### P5 · 신뢰 경계

- 읽기: `page.tsx`가 `lib/supabase/server.ts`로 읽는다. RLS `places_read`가 active 회원에게 SELECT를
  허용하므로 충분하고, admin 클라이언트는 쓰지 않는다 (§3).
- 쓰기: `actions/places.ts`. Server Action은 공개 엔드포인트다 (§17). 입력 형태는 `lib/placeResult.ts`의
  순수 검증 함수로, 인가는 액션 안에서 `auth.getUser()` → `profiles.status='active' && role='admin'`을
  다시 본다. 행 없음도 거부다 (fail-closed). `proxy.ts`의 `/admin` 가드는 화면 전환용이다 (§15).
  통과한 뒤에만 `lib/supabase/admin.ts`(service_role)로 INSERT/UPDATE한다.
- 클라이언트에서 받지 않는 것: `created_by`(서버 `user.id`), `id`·`is_active`(생성 시 DB default), 정확도.
- 결과 타입은 `{ ok: true, id } | { ok: false, reason: 'invalid_input' | 'unauthorized' | 'db_error', message }`.
  인증 없음도 `unauthorized`다 — 운영자 화면에 직접 POST한 사람에게 사유를 나눠 줄 이유가 없다.
  토글 UPDATE가 0행이면 `invalid_input`("새로고침해 주세요") — 목록이 낡은 것이지 DB 실패가 아니다.
- 성공 시 `revalidatePath('/admin/places')`. Server Function 안에서 부르면 같은 응답에 현재 라우트의 RSC
  payload가 다시 실리므로, 클라이언트는 `router.refresh()` 없이 props 갱신으로 목록을 받는다.
  낙관적 갱신은 하지 않는다.
- `app/admin/layout.tsx`는 만들지 않는다. 접근 제어는 `proxy.ts`(T14)가 이미 한다.

---

## 24. 임시 세션 생성 (T29)

`app/admin/sessions/new`(Server Component + `NewSessionForm` 클라이언트 자식)와
`actions/create-session.ts`. 운영자가 저장된 장소를 골라 반복 일정과 무관한 일회성 세션을 만든다.
결과는 `schedule_id = null`, `session_type = 'regular'`인 `sessions` 행이다.
**임시 세션과 번개의 차이는 §17** — 둘 다 `schedule_id`가 null이지만 가르는 것은 `session_type`이다.

### D1 · 라우트는 `/admin/sessions/new` 하나

세션 목록·취소·수정 화면(`/admin/sessions`)은 T31이다. 이 화면은 만들기만 한다.
접근 제어는 `proxy.ts`(T14)가 하고, 쓰기 인가는 액션 안에서 다시 본다.

### D2 · 폼은 장소 · 시각 · 반경 · 창 시작뿐

- 장소: `is_active = true`인 `places`만 select. 기본값은 이름순 첫 장소.
- 반경: 슬라이더 20~200m, 5m 단위. **장소를 고르면 그 장소의 `default_radius`로 초기화되고 이후 운영자가
  조정한다.** §17의 "반경은 서버 상수로 고정"은 번개(회원 개설)에만 해당한다 — 운영자가 여는 정규
  세션은 계절·날씨에 따라 반경을 바꾸는 게 §7의 의도다. 상수는 `lib/placeResult.ts`의 것을 재사용한다.
- 창 시작 `open_before_min`: 0~120 정수, 기본 10 (DB CHECK·default와 같다).
- **`open_after_min`은 폼에 없다. 서버가 10으로 박는다** (§5, §11 "창 길이 조정 UI" 금지).
- **`session_type`은 폼에 없다. 서버가 `'regular'`로 박는다** (§17).

### D3 · 시각은 KST 벽시계로 받고, 마감 전이면 과거 시각도 허용한다

`<input type="datetime-local">`의 값 `'YYYY-MM-DDTHH:mm'`을 클라이언트가 그대로 보낸다. 서버는 `'T'`로
나눠 `lib/kst.ts kstDateTime(date, time)`으로 KST Date를 만든다. **KST 산술은 kst.ts 한 곳에만 있다** —
액션에서 +09:00을 직접 더하지 않는다. 존재하지 않는 날짜(02-30)·25시는 `invalid_input`이다.

허용 조건은 **창이 아직 안 닫혔을 것** — `getWindow({ meetAt, openBeforeMin, openAfterMin: 10 }).closesAt > now`
(`lib/sessionResult.ts isWindowStillOpen`, now는 서버 시각). **과거 `meet_at`이라도 마감 전이면 허용한다.**
"지금 06:50 집합 열자"가 가능해야 하고, T22는 창이 열린 세션을 고르므로 (§20) 만들자마자 메인에 뜬다.
마감 정각은 닫힌 것이다 — `lib/window.ts`의 반열린 구간과 같은 방향이다. 마감된 시각이면 `window_closed`.
마감 여부를 클라이언트에서 미리 검사하지 않는다 — 클라이언트 시계는 판정이 아니다 (§3).

### D4 · 같은 시각의 세션이 있어도 막지 않는다

SELECT로 중복을 검사하지 않는다. 세 가지 근거다.
1. `sessions_schedule_slot` unique 인덱스는 `where schedule_id is not null`이라 임시 세션에는 걸리지 않는다.
   DB가 막지 않는 것을 액션이 SELECT로 흉내 내면 두 요청이 겹칠 때 둘 다 통과한다 (§19와 같은 이유).
2. 잘못 만든 세션은 운영자 취소(T31·T45, `canceled_at`)로 수습한다. 만들기를 막는 것보다 싸다.
3. T22의 선택 규칙이 결정적이다 — 창이 열린 세션 중 regular 우선 → `meet_at` 이른 순 (§20).
   같은 시각의 정규 세션이 둘 있어도 메인 화면은 흔들리지 않는다.

### D5 · 신뢰 경계

- 읽기: `page.tsx`가 `lib/supabase/server.ts`로 활성 장소를 읽는다 (RLS `places_read`). 좌표는 select하지
  않는다. admin 클라이언트는 쓰지 않는다 (§3).
- 쓰기: `actions/create-session.ts`. Server Action은 공개 엔드포인트다 (§17). 입력 형태는
  `lib/sessionResult.ts`의 순수 검증 함수로, 인가는 액션 안에서 `auth.getUser()` →
  `profiles.status='active' && role='admin'`을 다시 본다. 행 없음·미인증도 `unauthorized`다 (T27과 같다).
- **클라이언트가 보낸 장소 이름·좌표는 받지 않는다.** 액션이 `places`에서 `name·lat·lng`를 읽어
  `sessions`에 스냅샷으로 복사한다 (§7). 클라이언트는 `placeId`만 보내고, 그 id가 없거나 비활성이면
  `place_not_found`다. 클라이언트가 좌표를 보내게 두면 임의 좌표의 세션을 만들 수 있다 (§3).
- `created_by`는 서버 세션의 `user.id`. `console.error`에 좌표를 넣지 않는다 (§8).
- 결과 타입은 `{ ok: true, id, meetAt } | { ok: false, reason: 'invalid_input' | 'unauthorized' |
  'place_not_found' | 'window_closed' | 'db_error', message }`. `meetAt`은 DB가 돌려준 저장값이다.
- 성공 시 `revalidatePath('/')` — 메인의 `getNextSession` 결과가 바뀐다. `/admin/sessions/new` 자체에는
  갱신할 목록이 없다.
- `authorizeAdmin`은 `actions/places.ts`의 것과 같은 내용을 파일 안에 다시 둔다. `'use server'` 파일의
  export는 전부 공개 엔드포인트라 한쪽에서 꺼내 쓸 수 없다.

---

## 25. 버튼 클릭 연결 (T57)

`components/MainScreen.tsx`의 `SessionView`가 "집합하기" 버튼을 `actions/check-in.ts`의 `checkIn`에 연결한다.
§22의 4상태 판정 코드는 그대로 두고 그 위에 전송 중 · 완료 · 서버 결과 state를 얹는다.
**서버 게이트(§19)가 최종 판정이고, 클라이언트는 결과를 표시만 한다.**

### 1 · `useTransition` + `startTransition(() => checkIn(...))`

`useActionState`·`<form action>`은 쓰지 않는다. 결과를 state로 들고 버튼 위 1번째 줄에 얹는 구조라
폼 액션이 줄 것이 없고, 화면에는 제출할 필드도 없다. 인자 `{ sessionId, lat, lng, accuracy }`는
**클릭 시점에** `useGeolocation`의 최신 `position`에서 읽는다. 시각은 보내지 않는다 (§19).

### 2 · 전송 중

`isPending` 동안 버튼은 disabled, 라벨은 "확인 중…". 연타 방지는 클라이언트 1차이고,
최종 방어선은 서버의 `unique (session_id, member_id)`다 — 23505는 `already`로 돌아온다 (§19).

### 3 · 완료는 5번째 상태, 클라이언트 state뿐

`ok: true`와 `ok: true, already: true`는 같은 상태 **"집합 완료"**(완료 색 = 활성 축 green, disabled)다.
부원 입장에서 출석이 되어 있는 건 같다. 1번째 줄에는 서버 문구(`출석 완료` / `이미 출석 처리됐어요`)가
남는다 — already를 알려 주는 게 유용하다.

**완료 상태는 클라이언트 state로만 유지한다. 새로고침하면 다시 "집합하기"가 보이는 것은 의도된
동작이다.** 다시 누르면 서버가 `already`를 돌려주므로 두 번 기록되지 않는다. 페이지 로드 시
`check_ins`를 조회해 완료 상태를 복원하는 것은 T59로 분리한다 (선행: T13 authenticated SELECT RLS 검증).

### 4 · 서버 거절 문구는 1번째 줄, 게이트 문구보다 우선

`lib/checkInResult.ts`·게이트의 한국어 문구를 버튼 위 1번째 줄에 띄운다. 토스트·별도 카드는 만들지 않는다.
다음 탭 시도(핸들러 진입) 시 지운다. 우선순위는 `lib/checkInStatusLine.ts`(순수, vitest)에 한 곳으로 둔다.

```
서버 결과 문구 > 시뮬레이션 안내 > 게이트 사유(꺼져 있을 때) > GPS 안내(위치 없을 때) > 빈 줄
```

서버 문구가 최우선인 이유: 게이트가 "집합하기"라고 해도 서버가 거절했으면 그 이유가 보여야 하고,
완료 뒤 마감이 지나도 "출석 완료"가 "출첵 마감"에 덮이면 안 된다.

### 5 · sim 모드는 전송하지 않는다

`sim` prop이 있으면(§22, `/dev`의 `DevMainScreen`만) 액션을 부르지 않고 1번째 줄에
"시뮬레이션: 전송 안 함"을 띄운다. 서버가 거리를 재계산하므로 슬라이더 거리와 서버 판정이 어긋난다.
안내는 **탭한 뒤** 뜨고, 게이트 문구가 바뀌면(슬라이더·라디오) 사라진다 — 항상 띄우면 `/dev`의
4상태 확인(§22)이 죽는다. effect로 지우지 않고 탭 시점의 게이트 문구를 스냅샷해 비교한다.
