# 집합(Jiphap) — 작업 티켓

**티켓 하나 = 커밋 하나.** 여러 개를 묶어서 하지 않는다.

Claude Code에서 작업할 때는 이렇게 시작한다.

```
ARCHITECTURE.md와 SCHEMA.sql을 읽고, TASKS.md의 T06만 처리해줘.
다른 티켓은 건드리지 마.
```

각 티켓의 **완료 기준**을 실제로 확인하기 전에는 체크하지 않는다.

---

## Phase 0 — 기반 (배포 파이프라인부터 뚫는다)

빈 화면이라도 먼저 인터넷에 올린다. 마지막에 배포하려다 막히는 게 가장 흔한 실패다.

### T01 · Next.js 프로젝트 생성 ✓
`create-next-app` — App Router, TypeScript, Tailwind, ESLint.
**완료 기준:** `npm run dev`로 로컬에서 뜬다.

### T02 · Supabase 프로젝트 생성 + 환경변수 ✓
`.env.local` 구성 (ARCHITECTURE.md §12). `.gitignore` 확인.
**완료 기준:** `SUPABASE_SECRET_KEY`가 `NEXT_PUBLIC_` 접두사 **없이** `.env.local`에 있고,
`.env.example`만 커밋된다 (`.env.local`은 `.gitignore`에 걸린다).

### T03 · 스키마 마이그레이션 ✓
`SCHEMA.sql`의 테이블·트리거·함수 부분 적용 → `supabase/migrations/0001_schema.sql`.
RLS 켜기(`enable row level security`)는 0002가 아니라 0001에 둔다 — `create table`은 RLS를
켜지 않아, 0001만 적용된 구간 동안 테이블이 Data API에 전면 개방되기 때문이다
(RLS 켜짐 + 정책 0개 = 전면 차단).
**완료 기준:** 대시보드 테이블 에디터에 5개 테이블이 보인다.

### T04 · RLS 정책 적용
`SCHEMA.sql`의 RLS 부분 적용 → `supabase/migrations/0002_rls.sql` (`create policy`만. 켜기는 0001에서 끝났다).
0002는 T03 검증 시점에 이미 적용돼 있었다 — 정책 6개가 전부 `SELECT`/`to authenticated`로
확인됨(`anon` 대상 정책 0개). 재실행하면 `already exists`로 실패한다. 남은 건 완료 기준 확인뿐.
메모: T03 검증 (f) 결과 — `anon`·`authenticated` 모두 5개 테이블에 INSERT/UPDATE/DELETE/
TRUNCATE GRANT를 갖고 있다. 따라서 여기서 나오는 "거부됨"은 GRANT 부재가 아니라 RLS 때문이다.
동시에 RLS가 유일한 방어선이라는 뜻이기도 하다 — write GRANT를 revoke할지는 미결(§3에 결정 없음).
**완료 기준:** anon 키로 `check_ins` INSERT를 시도하면 거부된다.

### T05 · Vercel 배포
GitHub 저장소 연결, 환경변수 입력.
**완료 기준:** `https://<프로젝트>.vercel.app`이 HTTPS로 뜬다.

### T06 · 카카오 개발자 앱 등록
JavaScript 키(지도) + REST API 키(로그인) 발급. **플랫폼 도메인과 Redirect URI 양쪽** 등록.
**완료 기준:** Supabase Auth Providers에서 Kakao가 켜져 있다.

---

## Phase 1 — 계산 코어 (UI 없이, 테스트만)

**이 계산이 틀리면 나머지가 전부 무의미하다.** UI보다 먼저 한다.
전부 순수 함수. React·Supabase·브라우저 API에 의존하지 않는다.

### T07 · `lib/geo.ts` — 거리와 방위각
하버사인, 지구 반지름 6371000m. `bearing()`은 레이더에서 내 점의 각도로 쓴다.
**완료 기준:** 알려진 좌표쌍(예: 서울시청↔광주시청 약 267km) 테스트 통과. 동일 지점은 0.

### T08 · `lib/window.ts` — 세션 창 판정
`meet_at`, `open_before_min`, `open_after_min`, 현재 시각 → `'before' | 'open' | 'after'`.
**완료 기준:** 경계값(정확히 opens_at, 정확히 closes_at) 테스트 통과. KST 기준 확인.

### T09 · `lib/gate.ts` — 출첵 게이트
위치축 × 시간축 → `{ enabled, reason, message }`. ARCHITECTURE.md §5의 4상태 표 그대로.
**완료 기준:** 6개 조합 전부 테스트. 비활성일 때 `message`가 반드시 채워진다.

### T10 · 정확도 경고 로직
`accuracy > radius`일 때 경고 문구 반환. **판정 자체는 바꾸지 않는다.**
**완료 기준:** 오차가 커도 `enabled`가 그대로인 테스트 통과.

---

## Phase 2 — 인증

### T11 · Supabase 클라이언트 3종
`lib/supabase/client.ts`(브라우저), `server.ts`(RSC), `admin.ts`(service_role).
**완료 기준:** `admin.ts`에 `import 'server-only'`이 있다.

### T12 · 카카오 로그인 + 콜백
`signInWithOAuth`, `app/auth/callback/route.ts`에서 코드 교환.
**완료 기준:** 로그인 후 세션 쿠키가 생기고, `profiles`에 `pending` 행이 자동 생성된다.

### T13 · 승인 대기 화면 + 가드
`proxy.ts`에서 `status`별 분기. `pending`은 대기 화면만, 비로그인은 로그인으로.
**완료 기준:** pending 계정으로 `/`에 접근하면 대기 화면으로 리다이렉트된다.

### T14 · 운영자 가드
`proxy.ts`에서 `role='admin'`이 아니면 `/admin/*` 접근 차단.
**완료 기준:** 일반 회원이 `/admin/members`에 가면 막힌다.

---

## Phase 3 — 위치와 표시

### T15 · `useGeolocation` 훅
`watchPosition`, `enableHighAccuracy: true`. 언마운트 시 반드시 `clearWatch`.
권한 거부·타임아웃·`PERMISSION_DENIED`를 각각 다른 안내로 처리.
**완료 기준:** 권한을 거부해도 앱이 죽지 않고 안내 문구가 뜬다.

### T16 · `components/Radar.tsx`
SVG 원형 지오펜스. props만 받는 순수 컴포넌트.
바깥 원=반경(밖이면 점선+경고색, 안이면 실선+초록), 중심 십자=집합 장소,
내 점=거리와 방위각으로 배치(반경 초과 시 가장자리에 고정), 반투명 원=GPS 정확도.
**완료 기준:** `/dev` 페이지에서 props를 바꿔가며 모든 상태가 그려진다.

### T17 · 시뮬레이션 슬라이더 (개발 전용)
GPS 대신 임의 거리를 주입. 맨 왼쪽이 꺼짐. 프로덕션 빌드에서는 숨긴다.
**완료 기준:** 슬라이더로 반경 경계를 넘나들면 버튼이 정확히 그 지점에서 열리고 닫힌다.

### T18 · `components/KakaoMap.tsx`
SDK 동적 로드, `kakao.maps.Circle`(반경), 마커(집합 장소), 내 위치, 반투명 원(오차).
**완료 기준:** 지도 위에 반경 원과 내 위치가 함께 보인다.

### T19 · 지도 실패 시 레이더 폴백
SDK 로드 실패·타임아웃을 감지해 자동 전환.
**완료 기준:** 네트워크를 끊어도 레이더가 뜨고 거리가 갱신된다.

### T20 · 길찾기 딥링크
카카오맵 앱 URL 스킴. SDK 불필요.
**완료 기준:** 폰에서 누르면 카카오맵이 열리고 목적지가 찍혀 있다.

---

## Phase 4 — 출첵 (핵심)

### T21 · `actions/check-in.ts` Server Action
서버 시각으로 창 검증 → 거리 재계산 → 회원 자격 → 중복 확인 → INSERT.
**원좌표는 저장하지 않는다.** `dist_m`, `accuracy_m`만 남긴다.
**완료 기준:** 창 밖 시각으로 호출하면 거부된다. 두 번 호출하면 unique 제약에 걸린다.

### T22 · 다음 세션 조회
지금 기준 가장 가까운 미취소 세션. 없으면 "다음 집합: 화요일 06:50" 안내.
**완료 기준:** 세션이 없는 날에도 화면이 비어 보이지 않는다.

### T23 · 메인 화면 조립
세션 헤더 / 지도 / 거리 숫자 / 상태 문구 / 버튼 / 명단. **스크롤 없는 한 화면.**
거리 숫자와 버튼은 지도 위 오버레이.
**완료 기준:** iPhone SE 크기(375×667)에서 스크롤 없이 전부 보인다.

### T24 · 버튼 4상태 UI
`lib/gate.ts` 결과를 그대로 렌더. **비활성 이유가 버튼 바로 위에 항상 뜬다.**
**완료 기준:** 시뮬레이션 슬라이더로 4상태를 전부 눈으로 확인.

### T25 · 오늘의 명단 + Realtime
이름 / 시각 / 거리 / 지각 표시. `check_ins` 구독.
**완료 기준:** 다른 브라우저에서 출첵하면 새로고침 없이 명단에 나타난다.

---

## Phase 5 — 운영자

### T26 · 회원 승인 화면
pending 목록 → 승인 / 거절.
**완료 기준:** 승인하면 그 계정이 즉시 앱을 쓸 수 있다.

### T27 · 장소 등록
**"지금 내 위치를 집합 장소로 지정"** + 지도 핀 찍기 + 저장된 장소 목록.
위도·경도 직접 입력 UI는 만들지 않는다.
**완료 기준:** 현장에서 버튼 하나로 장소가 등록된다.

### T28 · 반복 일정 CRUD
요일 다중 선택, 시각, 창(기본 ±10분), 반경 슬라이더(20~200m, 기본 60m).
**완료 기준:** 매주 화/목/토 06:50 규칙을 만들 수 있다.

### T29 · 임시 세션 생성
반복과 무관한 일회성 집합.
**완료 기준:** `schedule_id`가 null인 세션이 만들어진다.

### T30 · 수동 출첵
배터리·GPS 문제로 억울한 결석이 생기지 않게 한다. **없으면 안 되는 기능이다.**
`method='manual'`, `created_by`에 운영자 기록.
**완료 기준:** 명단에 수동 출첵이 구분되어 표시된다.

### T31 · 세션 현황
출석 수 / 정회원 수를 **숫자로만** 표시한다 (예: `출석 12 / 정회원 30`).
**미도착자를 이름으로 나열하지 않는다.** 앱이 결석을 선고하지 않는다 —
누가 왜 안 왔는지는 운영자가 판단할 일이다.
미도착자의 위치도 표시하지 않는다 (ARCHITECTURE.md §8).
**완료 기준:** 화면에 미도착자의 이름이 하나도 나오지 않고, 출석 수와 정회원 수가 맞는다.

---

## Phase 6 — 운영

### T32 · 일일 크론
`/api/cron/daily` — 향후 14일치 세션 생성 + Supabase keep-alive 겸용.
`CRON_SECRET` Bearer 검증. `vercel.json`에 `"schedule": "0 20 * * *"` (= KST 05:00).
**완료 기준:** 수동 호출 시 세션이 생성되고, 두 번 호출해도 중복 생성되지 않는다.

### T33 · 내 기록 / 출석률
개인별 참석 횟수와 비율.
**완료 기준:** 분모가 해당 기간의 전체 세션 수로 계산된다 (ARCHITECTURE.md §6).

### T34 · PWA
manifest, 아이콘, 홈화면 추가 안내.
**완료 기준:** 폰에서 홈화면에 추가하면 주소창 없이 뜬다.

### T35 · 다크모드 · 대형 탭 영역 점검
장갑 낀 손 기준으로 전체 훑기.
**완료 기준:** 모든 버튼의 탭 영역이 최소 48×48px.

### T36 · 명단 텍스트 복사
오늘의 출첵 명단을 단톡방에 붙여넣을 수 있는 플레인 텍스트로 클립보드에 복사한다.
**방향은 앱 → 카톡 한쪽뿐이다.** 카톡에서 읽어오는 기능은 만들지 않는다 (ARCHITECTURE.md §11).
**완료 기준:** 버튼을 누르면 이름과 시각이 줄바꿈된 텍스트가 클립보드에 복사된다.

---

## 현장 검증 (코드 밖의 일)

### V01 · 반경 측정
용봉탑 앞에서 "지금 내 위치로 지정"을 누르고 **몇 분간 서서 `정확도 ±Nm`를 관찰**한다.

**반경은 추측할 값이 아니라 측정해서 정할 값이다.**
오차가 ±25m로 찍히면 60m가 적당하고, ±40m가 나오면 80m 이상으로 올린다.

### V02 · 실제 걷기 테스트
반경 밖에서 걸어 들어가며 거리가 갱신되고 버튼이 열리는 지점을 확인한다.

### V03 · 실제 집합 1회 운영
5명 이상으로 아침에 한 번 돌려본다. 여기서 나오는 문제가 진짜 문제다.

---

## 의존 관계

```
Phase 0 ──> Phase 1 ──> Phase 2 ──> Phase 3 ──> Phase 4 ──> Phase 5 ──> Phase 6
                │                        │           │
                └── T09가 T24를 결정 ──────┘           └── T21 없이는 아무것도 기록 안 됨

V01은 T27 이후 아무 때나. 단 V03 전에는 반드시.
```
