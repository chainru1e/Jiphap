# CLAUDE.md

전남대 러닝 동아리(CNRC) GPS 출석 체크 웹앱 **집합(Jiphap)**.

## 작업 전 반드시 읽을 것

- `ARCHITECTURE.md` — 결정 사항의 기록. 판단이 필요하면 여기부터 본다
- `TASKS.md` — 티켓 목록. 지금 무엇을 하는 중인지
- `SCHEMA.sql` — DB 구조

**이 세 파일에 없는 것은 아직 결정되지 않은 것이다. 임의로 정하지 말고 물어본다.**

---

## 작업 방식

1. **한 번에 티켓 하나만.** 지시받은 티켓 외의 파일은 건드리지 않는다
2. 시작 전에 무엇을 할지 한 문단으로 말하고 확인을 받는다
3. **`완료 기준`을 실제로 확인하기 전에는 끝났다고 하지 않는다.** 확인할 수 없으면 확인할 수 없다고 말한다
4. 티켓 하나가 커밋 하나. 커밋 메시지는 `T09: 출첵 게이트 함수` 형식.
   **커밋은 사람이 한다.** Claude는 메시지 초안만 제시하고 직접 실행하지 않는다
5. 작업이 끝나면 `TASKS.md`의 해당 티켓 제목 끝에 `✓`를 붙인다 (`### T01 · ... ✓`)

## 하지 말 것

- **문서에 없는 기능을 추가하지 않는다.** "있으면 좋을 것 같아서" 넣지 않는다
- 티켓 범위를 넘어 리팩터링하지 않는다
- `ARCHITECTURE.md` §11(하지 않을 것)을 위반하지 않는다
- 결정이 필요한 지점에서 혼자 정하고 넘어가지 않는다. 멈추고 묻는다
- 완료 기준을 확인하지 않고 "완료했습니다"라고 쓰지 않는다
- **`git commit` · `git push` · `git tag`를 실행하지 않는다.** 형식에 맞는 메시지를
  제안하는 것까지가 Claude의 역할이고, 저장소에 남기는 행위는 사람이 한다

---

## 절대 규칙 (위반 시 전부 무의미)

### 보안

- `SUPABASE_SERVICE_ROLE_KEY`는 서버 전용. **`NEXT_PUBLIC_` 접두사 금지.** `admin.ts`에는 `import 'server-only'`
- 클라이언트에서 DB에 쓰지 않는다. 모든 쓰기는 Server Action → service_role
- `check_ins`, `sessions`, `profiles`에 클라이언트용 INSERT/UPDATE/DELETE 정책을 만들지 않는다

### 출첵 판정

- **출첵 시각은 서버의 `now()`.** 클라이언트가 보낸 시각을 절대 쓰지 않는다
- 클라이언트의 거리 계산은 버튼을 켜고 끄기 위한 것일 뿐. 서버가 반드시 다시 계산한다
- **GPS 정확도를 이유로 출첵을 막지 않는다.** 경고만 띄운다. 실제로 도착한 사람을 막는 게 오차보다 큰 문제다
- 지각 여부를 저장하지 않는다. `checked_at > meet_at`으로 표시할 때 계산한다

### 프라이버시

- **원좌표(lat/lng)를 저장하지 않는다.** 검증에만 쓰고 `dist_m`, `accuracy_m`만 남긴다
- 미도착자의 위치를 수집·저장·표시하지 않는다. 요청이 와도 만들지 않는다

---

## 코드 규칙

- `lib/geo.ts`, `lib/window.ts`, `lib/gate.ts`는 **순수 함수만.** React·Supabase·브라우저 API를 import하지 않는다. 테스트가 쉬워야 한다
- `useGeolocation`은 언마운트 시 반드시 `clearWatch`
- 에러를 삼키지 않는다. 위치 권한 거부·타임아웃·SDK 로드 실패를 각각 다른 안내로 처리한다
- 주석은 "무엇"이 아니라 "왜"를 쓴다. 특히 위 절대 규칙과 관련된 곳

## UI 규칙

- **모든 문구는 한국어. 존댓말, 짧게.** 영어 문구를 남기지 않는다
- **아침 7시에 밖에서 장갑 낀 손으로 쓰는 앱이다.** 탭 영역 최소 48×48px
- 색은 두 축만: **대기(반경 밖)** 와 **활성(반경 안)**
- 버튼이 비활성일 때 **이유가 버튼 바로 위에 항상 뜬다.** "왜 안 눌리지"가 이 앱의 유일한 실패 모드
- 다크모드 대응
- 사용자 메인은 스크롤 없는 한 화면 (375×667 기준)

---

## 기술 스택 (변경 금지)

Next.js (App Router) · TypeScript · Tailwind · Supabase · 카카오맵 JS SDK · Vercel

바꾸고 싶은 이유가 생기면 바꾸지 말고 말한다. 근거는 `ARCHITECTURE.md` §2에 있다.

@AGENTS.md

## 명령어

```bash
npm run dev     # 개발 서버
npm test        # 테스트 (Phase 1 이후 반드시 통과 상태 유지)
npm run build   # 빌드 확인
```
